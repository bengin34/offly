export const hideProUpgradeBanner = __DEV__ && true;
export const hideVaultFreeLimit = __DEV__ && true;

// Set to true to wipe and re-seed all mock memories on next launch.
// Flip back to false once the photos look correct.
export const forceMockReseed = __DEV__ && true;
